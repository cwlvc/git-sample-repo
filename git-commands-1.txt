git commands
--------------------
git init ---- helps initializing the folder of git so it recognizes it as a git folder
--------------------
for don't know who you are issue:
1.git init
2.git config user.name "someone"
3.git config user.email "someone@someplace.com"
4.git add *
5.git commit -m "some init msg"
---------------------
cls - clear file